{
  "name": "uk-ferry-map",
  "version": "1.0.0",
  "description": "Interactive map of ferry routes to and from the UK",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node scripts/build.js --check",
    "start": "node scripts/build.js && npx --yes serve docs"
  },
  "engines": { "node": ">=18" },
  "dependencies": { "d3-geo": "^3.1.1" },
  "license": "MIT"
}
